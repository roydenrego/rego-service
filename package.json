{
  "name": "rego-service",
  "version": "1.0.0",
  "description": "Micro-services that power the roydenrego.com React App.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Royden Rego",
  "license": "ISC",
  "dependencies": {
    "@sendgrid/mail": "^6.4.0",
    "dotenv": "^8.2.0",
    "mongoose": "^5.8.7",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "serverless-domain-manager": "^3.3.1",
    "serverless-offline": "^12.0.4"
  }
}
